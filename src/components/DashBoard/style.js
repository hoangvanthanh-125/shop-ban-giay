import { makeStyles } from "@material-ui/core";
export default makeStyles((theme) => ({
  wrapper: {},
  all: {
    position: "relative",
  },
  header: {
    position: "sticky",
    top: 0,
    zIndex: 100,
  },
  wrapSort: {
    position: "relative",
  },
}));
